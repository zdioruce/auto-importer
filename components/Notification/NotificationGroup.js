import styles from './NotificationGroup.module.scss'

export default function NotificationGroup(props) {

    return (
        <div className={styles.gIdkRj}>
            {props.children}
        </div> 
    )
}
